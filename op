import numpy as np
from scipy.optimize import minimize

def hedef_fonksiyon(x):
    return (x[0] - 4)**2 + (x[1] - 3)**2

x0 = np.array([0, 0])

sonuc = minimize(hedef_fonksiyon, x0, method='BFGS')

print("Optimum değerler:", sonuc.x)
print("Minimum değer:", sonuc.fun)
